# 自我审计清单（2026-09-11）

审计方式：三路并行新鲜眼睛代理（核心改动回归 / 面板表单与 UI/UX / 开源用户旅程与文档）+ 本地回归测试（965 项）。
本文件记录**未在本轮修复**的遗留项，按优先级排列；已修项见对应提交。

## 已修复（本轮）

- chat-protocol：官方调用注记时序回归（正文缓冲导致注记被排到正文之后）
- auth-manager：余量选号在 lead 优先级全体耗尽时饿死健康低优先级账号；ties 返回取错属性（.acc）
- router-handler：429 冷却文案"窗口余量"实为"已用百分比"误导；日志 account 字段补 8 位前缀
- admin-ui（v1.10.2 系列）：apply-router 超时 15s→180s；设置页压缩阈值保存未接线（本轮补）
- admin-ui（本轮）：models 编辑上下文简写（1M/272k）静默丢弃 → 显式报错；厂商编辑地址变更批量下发兄弟通道；OAuth 手动导入默认直连（不再预填 10808 死代理）
- README：Windows 执行策略放行命令；移除 "npm start 直接跑" 的不实承诺

## 遗留 P1

- [ ] keys/dashboard 接入示例 base_url 硬编码 15730，应取 /status 的 routerBaseUrl 动态渲染（views/keys/Index.vue:399-428、dashboard/Index.vue:22）
- [ ] Topbar「测试所有模型连接」在非 /models 页触发时事件先于监听器注册而丢失，testingAll 永远转圈（layout/Topbar.vue:76-85）
- [ ] ChannelKeyPoolDialog 编辑态"留空表示不修改 Key"与"空值直接拒绝"自相矛盾（components/ChannelKeyPoolDialog.vue:182-184 vs 320-325）
- [ ] ProxyConfigEditor 自定义代理字段不完整时静默落成直连，应警告（components/ProxyConfigEditor.vue:132-135 + views/models/Index.vue:751-756）
- [ ] settings loadDesktopState 失败时头部标签误显示"官方直连"（views/settings/Index.vue:950-967）
- [ ] settings handleVisionSave / models loadRequestLog 等多处 skipGlobalError + 静默 catch，失败无提示
- [ ] OAuthDialog 确认绑定无 loading/防重入（components/OAuthDialog.vue:135）
- [ ] 入库 config.json 官方通道默认 viaProxy:true（假设本机 10808 代理）且含大量作者个人 Google 订阅通道，应精简为最小模板并在 README 注明代理假设

## 遗留 P2

- [ ] 多处 catch 内手动 ElMessage.error 与拦截器 toast 双重提示（settings:809、keys:352、subscriptions:518、models:947 等）
- [ ] models handleTestAll 批量测速逐个弹窗/toast 刷屏，应聚合展示（views/models/Index.vue:1591-1617）
- [ ] models auto_compact_token_limit 非法输入静默省略，与 settings 页行为不一致（views/models/Index.vue:1714-1716）
- [ ] models 分组头 apiBase 长地址无 truncate（views/models/Index.vue:87-93）
- [ ] settings 视觉中继卡片"运行中"标签为硬编码假状态（views/settings/Index.vue:69）
- [ ] components/TargetEditorDialog.vue 为死组件，可删
- [ ] start-router.ps1 缺 Node 版本前置检查（.sh 版有）；repoRoot 计算多一层 Split-Path（scripts/start-router.ps1:43，靠第二候选兜底）
- [ ] ADVANCED.md Cursor 网关"随路由启动"需 CURSOR_KEY 前提，文档未注明（docs/ADVANCED.md:107）
- [ ] account-quota：retry-after 亚秒值无下限钳制；used=100 且 resetsAt 缺失时按 15s 突发循环；双窗口 windowMinutes 相同时的二次折减排序可能失真
- [ ] chat-stream function_call_arguments.delta 帧未随 done 帧同步解包（codex 客户端只消费 done，影响面小）

## 已核实无需处理

- DeepSeek 思考模式会话级 reasoning_content 校验（v1.10.5 合并形态修复，真实对话重放 200）
- 429 冷却分型（v1.10.3）：窗口耗尽（used≥100）停至重置点；突发按 retry-after/15s 短冷却
- 额度耗尽期"剩 2% 仍 429"= 上游准入拒绝（该请求预计消耗超过剩余额度），非路由缺陷
